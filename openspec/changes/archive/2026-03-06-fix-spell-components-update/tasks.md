## 1. Implement Bug Fix

- [x] 1.1 Update `heading_pattern` regex in `app.py` (`parse_spell_data` function) to support commas, numbers, and smart quotes.

## 2. Verification

- [x] 2.1 Run local test against Row 50 (Break Enchantment) to verify the fix works and components M and DF are no longer incorrectly extracted.
- [x] 2.2 Ensure the change does not break other spell extraction.

