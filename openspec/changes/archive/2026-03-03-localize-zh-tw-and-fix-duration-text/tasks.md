## 1. 環境依賴安裝

- [x] 1.1 在 `pyproject.toml` 中加入 `opencc-python-reimplemented` 依賴
- [x] 1.2 執行 `uv add opencc-python-reimplemented` 安裝套件並更新 `uv.lock`

## 2. 實作繁體中文轉換功能

- [x] 2.1 在 `app.py` 頂部引入 `opencc` 模組，並初始化 `OpenCC('s2twp')` 轉換器
- [x] 2.2 建立 helper 函式 `to_zh_tw(text: str) -> str`，對傳入文字執行簡繁轉換後回傳

## 3. 套用轉換至資料更新流程

- [x] 3.1 在 `app.py` 中，找到所有對 `[持續時間]`、`[施法時間]`、`[施法距離]`、`[豁免說明]`、`[法抗]` 等欄位賦值的程式碼段
- [x] 3.2 在各欄位值被加入更新字典前，使用 `to_zh_tw()` 包裹文字

## 4. 修正持續時間字串處理

- [x] 4.1 在 `app.py` 中找到 `[持續時間]` 的解析邏輯，新增：先將文字中「每等級」取代為「每CL」
- [x] 4.2 確認截斷邏輯使用正確的長度門檻：替換後長度 > 16 字才回傳「其他」，否則保留替換後的文字

## 5. 驗證

- [x] 5.1 手動執行腳本，以少量測試資料（如 1~2 筆）驗證繁體轉換是否正確寫入試算表
- [x] 5.2 確認含「每CL」字串的持續時間能正確保留，不被歸為「其他」
